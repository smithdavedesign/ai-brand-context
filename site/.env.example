# Site environment template — copy to .env and edit.
# PUBLIC_ vars are embedded into the client-side bundle at build time.

# Public URL of the Solidigm Brand MCP server
# Local dev:    http://localhost:8080
# Internal:     https://brand-mcp.internal.solidigm.com
PUBLIC_MCP_URL=http://localhost:8080
